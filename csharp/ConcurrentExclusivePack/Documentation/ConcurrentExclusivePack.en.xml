<?xml version="1.0"?>
<doc>
    <assembly>
        <name>ConcurrentExclusivePack</name>
    </assembly>
    <members>
        <member name="T:IntomicLib.ConcurrentExclusiveLock">
             <summary>
             Provides a high-performance, non-recursive synchronization lock based on Concurrent / Exclusive access permissions.
             </summary>
             <remarks>
             This lock expresses access permissions rather than read/write intent.
             Concurrent Concurrent means that the current operation may enter together with other Concurrent operations;
             Exclusive Exclusive means that the current operation must enter exclusively and cannot run concurrently with any other operation.
             This lock does not provide recursive nesting privileges.
            
             This lock supports preemptive Exclusive acquisition, in-place upgrade from Concurrent to Exclusive,
             Exclusive in-place downgrade from Exclusive to Concurrent, and observational snapshots of state and contention.
            
             Concurrent Ordinary Concurrent acquisition and release primarily use lightweight atomic counting and do not enter the Monitor scheduling queue.
             Ordinary Exclusive acquisition, as well as conversion from Concurrent to Exclusive,
             uses the mutual exclusion, waiting, signaling, and ordering mechanisms of Monitor for exclusive scheduling.
            
             This lock does not guarantee strict FIFO ordering or stronger scheduling fairness than Monitor.
             The actual execution order of threads is still affected by operating-system scheduling, CPU topology, cache state, system load,
             and the duration of caller business logic.
            
             This type is intended for synchronous, non-recursive, highly concurrent shared-state scenarios,
             especially server-side scenarios with many fine-grained lock instances.
             
             Exclusive Exclusive permission is thread-affine.
             After acquiring Exclusive, it must be released or downgraded by the same thread that acquired it;
             an execution flow that depends on Exclusive permission must not cross an <c>await</c>.
            
             This type is a value type, and a default-initialized instance is unusable.
             Use <see cref="M:IntomicLib.ConcurrentExclusiveLock.Create"/> to create an instance.
             </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLock.MaxConcurrent">
            <summary>
            The maximum number of Concurrent holders supported by a single lock instance.
            </summary>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.Create">
             <summary>
             Creates a correctly initialized <see cref="T:IntomicLib.ConcurrentExclusiveLock"/> instance.
             </summary>
             <remarks>
             <see cref="T:IntomicLib.ConcurrentExclusiveLock"/> <see cref="T:IntomicLib.ConcurrentExclusiveLock"/> is a value type, and a default-initialized instance is unusable.
             Always use this method to create a new instance.
            
             Copying the returned <see cref="T:IntomicLib.ConcurrentExclusiveLock"/> value does not copy the lock state;
             the copied value still references the same internal lock state.
             </remarks>
        </member>
        <member name="P:IntomicLib.ConcurrentExclusiveLock.ObservedState">
             <summary>
             An observational snapshot of the current lock state.
             </summary>
             <remarks>
             This value only represents the state observed at the instant of the read. It must not be used as an authoritative state check and is intended only for diagnostics and monitoring.
            
             After a preemptive Exclusive request enters the contention window, this value may already be observed as Exclusive
             even while the request is still waiting for current Concurrent holders to release.
             Therefore, ObservedState represents the lock's current access tendency or transition state;
             it does not mean that a thread is already executing Exclusive business code.
             </remarks>
        </member>
        <member name="P:IntomicLib.ConcurrentExclusiveLock.ObservedContention">
             <summary>
             Gets an observational indicator of the current contention pressure on the lock.
             </summary>
             <remarks>
             This value is a snapshot of contention pressure observed at the instant of the read and is intended only for diagnostics, monitoring, or scheduling reference.
            
             Therefore, this value is 0 in a purely Concurrent scenario;
             once Exclusive pressure exists, it returns the currently observed scale of Concurrent + Exclusive pressure.
             </remarks>
        </member>
        <member name="P:IntomicLib.ConcurrentExclusiveLock.ContextID">
             <summary>
             Atomically gets or sets the business context ID associated with the current lock.
             </summary>
             <remarks>
             This property records additional business state outside the lock protocol,
             for example, the current business context so that identical contexts can be recognized.
            
             Setting this property directly unconditionally overwrites the current ContextID.
             To determine whether a different context was applied, use <see cref="M:IntomicLib.ConcurrentExclusiveLock.SwitchContextID(System.Int32)"/>.
            
             A value of 0 means that no context ID is set.
             The meaning, allocation, validation, and cleanup of nonzero values are the caller's responsibility.
             </remarks>
        </member>
        <member name="P:IntomicLib.ConcurrentExclusiveLock.EpochID">
             <summary>
             Atomically gets or sets the business phase ID associated with the current lock.
             </summary>
             <remarks>
             This property records additional business state outside the lock protocol, such as the lifecycle phase of an exclusive region,
             a data version, processing batch, or another business phase.
            
             Setting this property directly unconditionally overwrites the current EpochID;
             it does not check whether the phase advances and does not prevent rollback or reset.
             To ensure that the phase ID can only increase, use <see cref="M:IntomicLib.ConcurrentExclusiveLock.RaiseEpochID(System.Int32)"/>.
            
             A value of 0 means that no phase ID is set.
             The meaning, allocation, validation, and cleanup of nonzero values are the caller's responsibility.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.SwitchContextID(System.Int32)">
            <summary>
            Sets a new ContextID and returns whether this operation changed the previous value.
            Returns false if the new value is the same as the previous value.
            </summary>
            <remarks>
            This method only switches the business context ID.
            </remarks>
            <param name="newContextID">The new context ID.</param>
            <returns>True if ContextID was changed to a different value; false if the new value was already current.</returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.RaiseEpochID(System.Int32)">
            <summary>
            Attempts to advance EpochID to a new phase and returns whether the advance succeeded.
            Returns false if the new value is less than or equal to the current value.
            </summary>
            <remarks>
            This method only advances the business phase ID.
            </remarks>
            <param name="newEpochID">The new phase ID. It must be greater than the current EpochID for the advance to succeed.</param>
            <returns>True if EpochID was successfully advanced to a greater value; otherwise, false.</returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.AcquireConcurrent(System.Int32)">
             <summary>
             Waits to acquire Concurrent access permission.
             </summary>
             <remarks>
             This method returns when entry into the Concurrent state is permitted.
            
             After acquiring Concurrent, the caller must later call <c>ReleaseConcurrent()</c> to release it;
             if another protocol subsequently changes the held state, release according to the resulting state.
            
             <paramref name="maxConcurrent"/> <paramref name="maxConcurrent"/> limits the maximum Concurrent ID that this acquisition may obtain.
             <paramref name="maxConcurrent"/> <paramref name="maxConcurrent"/> must be at least 1.
             </remarks>
             <param name="maxConcurrent">
             The maximum permitted Concurrent ID.
             Valid return values are in the range [1, maxConcurrent].
             </param>
             <returns>
             Returns the Concurrent ID obtained by this successful entry, in the range [1, maxConcurrent].
             IDs obtained by uninterrupted concurrent acquisitions in the same round are distinct; after a release, a later acquisition may reuse a previously returned ID.
             </returns>
             <exception cref="T:System.ArgumentException">Thrown when <paramref name="maxConcurrent"/> is less than 1.</exception>
             <exception cref="T:IntomicLib.ConcurrentExclusiveLockCapacityExceededException">
             Thrown when the number of Concurrent holders present at runtime exceeds the internal limit.
             The current implementation supports a 31-bit concurrent count space; this limit is effectively unreachable in normal environments.
             </exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.TryAcquireConcurrent(System.Int32)">
             <summary>
             Attempts to acquire Concurrent access permission.
             </summary>
             <remarks>
             This method checks only once whether Concurrent entry is currently possible and does not wait for the lock state to change.
            
             A nonzero return value means Concurrent was acquired, and the caller must later call <c>ReleaseConcurrent()</c> to release it;
             if another protocol subsequently changes the held state, release according to the resulting state.
            
             <paramref name="maxConcurrent"/> <paramref name="maxConcurrent"/> limits the maximum Concurrent ID that this acquisition may obtain.
             <paramref name="maxConcurrent"/> <paramref name="maxConcurrent"/> must be at least 1.
             </remarks>
             <param name="maxConcurrent">
             The maximum permitted Concurrent ID.
             Successful return values are in the range [1, maxConcurrent].
             </param>
             <returns>
             Returns the Concurrent ID obtained by this successful entry, in the range [1, maxConcurrent].
             IDs obtained by uninterrupted concurrent acquisitions in the same round are distinct; after a release, a later acquisition may reuse a previously returned ID.
             Returns 0 if Concurrent was not acquired.
             </returns>
             <exception cref="T:System.ArgumentException">Thrown when <paramref name="maxConcurrent"/> is less than 1.</exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.TryAcquireConcurrent(System.Int32,System.Int32)">
             <summary>
             Attempts to acquire Concurrent access permission within the specified time.
             </summary>
             <remarks>
             This method waits, within the specified timeout, for the lock to enter a state in which Concurrent can be acquired.
            
             A nonzero return value means Concurrent was acquired, and the caller must later call <c>ReleaseConcurrent()</c> to release it;
             if another protocol subsequently changes the held state, release according to the resulting state.
            
             <paramref name="millisecondsTimeout"/> When <paramref name="millisecondsTimeout"/> is 0, the method tries once without waiting; a negative value means an infinite wait.
            
             <paramref name="maxConcurrent"/> <paramref name="maxConcurrent"/> limits the maximum Concurrent ID that this acquisition may obtain.
             <paramref name="maxConcurrent"/> <paramref name="maxConcurrent"/> must be at least 1.
             If <paramref name="millisecondsTimeout"/> is nonnegative, the method waits at most until the timeout and then returns 0;
             if <paramref name="millisecondsTimeout"/> is negative, it waits indefinitely.
             </remarks>
             <param name="millisecondsTimeout">
             The maximum number of milliseconds to wait for Concurrent.
             0 0 means try once without waiting; a negative value means wait indefinitely.
             </param>
             <param name="maxConcurrent">
             The maximum permitted Concurrent ID.
             Successful return values are in the range [1, maxConcurrent].
             </param>
             <returns>
             Returns the Concurrent ID obtained by this successful entry, in the range [1, maxConcurrent].
             IDs obtained by uninterrupted concurrent acquisitions in the same round are distinct; after a release, a later acquisition may reuse a previously returned ID.
             Returns 0 if Concurrent was not acquired within the specified time.
             </returns>
             <exception cref="T:System.ArgumentException">Thrown when <paramref name="maxConcurrent"/> is less than 1.</exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.ReleaseConcurrent">
             <summary>
             Releases one currently held Concurrent access permission.
             </summary>
             <remarks>
             This method must be called from a context that currently holds Concurrent.
            
             After this call, the current context no longer holds that Concurrent access permission
             and must not continue executing business code that depends on it.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.AcquireExclusive">
             <summary>
             Waits to acquire Exclusive access permission.
             </summary>
             <remarks>
             This method requests preemptive Exclusive access.
            
             After the request is issued, the lock blocks new Concurrent entries
             and waits for current Concurrent holders to release.
             This method returns after the caller has actually acquired Exclusive access permission.
            
             After acquiring Exclusive, the caller must later call <c>ReleaseExclusive()</c> to release it,
             or downgrade through <c>ExclusiveToConcurrent()</c> and then release according to the Concurrent protocol.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.TryAcquireExclusive(System.Boolean)">
             <summary>
             Attempts to acquire Exclusive access permission.
             </summary>
             <remarks>
             When <paramref name="preemptConcurrent"/> is true,
             this method requests Exclusive preemptively and may wait.
            
             After entering exclusive contention, the request blocks new Concurrent and ordinary Exclusive entries.
             The method waits for callers that currently hold Concurrent permission to release it,
             then returns true after Exclusive has been acquired; the caller then holds Exclusive permission.
            
             If a Concurrent to Exclusive upgrade request appears during contention,
             the current request leaves contention and returns false,
             yielding exclusive scheduling to the upgrade request.
            
             When <paramref name="preemptConcurrent"/> is false,
             this method does not preempt Concurrent and does not wait for the lock state to change;
             Exclusive can be acquired only when the lock is Idle.
            
             After acquiring Exclusive, the caller must later call <c>ReleaseExclusive()</c> to release it,
             or downgrade through <c>ExclusiveToConcurrent()</c> and then release according to the Concurrent protocol.
             </remarks>
             <param name="preemptConcurrent">
             true True requests Exclusive preemptively;
             false false does not preempt Concurrent and attempts to acquire Exclusive only when the lock is Idle.
             </param>
             <returns>
             true True if Exclusive was acquired;
             false false if Exclusive was not acquired.
             </returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.TryAcquireExclusive(System.Int32)">
             <summary>
             Attempts to acquire Exclusive access permission within the specified time.
             </summary>
             <remarks>
             This method attempts to acquire preemptive Exclusive within the specified timeout.
            
             <paramref name="millisecondsTimeout"/> When <paramref name="millisecondsTimeout"/> is 0, the method tries once without waiting;
             a negative value means an infinite wait.
            
             A return value of true means Exclusive was acquired, and the caller must later call <c>ReleaseExclusive()</c> to release it,
             or downgrade through <c>ExclusiveToConcurrent()</c> and then release according to the Concurrent protocol.
            
             A return value of false means Exclusive was not acquired within the specified time,
             and the caller must not execute business code that depends on Exclusive permission.
             </remarks>
             <param name="millisecondsTimeout">
             The maximum number of milliseconds to wait for Exclusive.
             0 0 means try once without waiting; a negative value means wait indefinitely.
             </param>
             <returns>
             true True if Exclusive was acquired;
             false false if Exclusive was not acquired within the specified time.
             </returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.ReleaseExclusive">
             <summary>
             Releases the currently held Exclusive access permission.
             </summary>
             <remarks>
             This method must be called from a context that currently holds Exclusive.
            
             After this call, the current context no longer holds Exclusive access permission
             and must not continue executing business code that depends on it.
            
             After release, the lock returns to normal contention,
             and waiting Concurrent or Exclusive requests continue attempting entry according to the lock's contention rules.
            
             To retain Concurrent access after ending Exclusive,
             call <c>ExclusiveToConcurrent()</c>
             instead of this method.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.ExclusiveToConcurrent">
             <summary>
             Downgrades the currently held Exclusive access permission to Concurrent access permission.
             </summary>
             <remarks>
             This method must be called from a context that currently holds Exclusive.
            
             After this call, the current context no longer holds Exclusive access permission
             and must not continue executing business code that depends on it.
            
             The current context instead holds Concurrent access permission
             and must later call <c>ReleaseConcurrent()</c> to release it.
            
             If the current Exclusive permission was acquired through an ordinary Exclusive request,
             the downgrade retains Concurrent continuously, avoiding the access window caused by releasing and reacquiring.
            
             If the current Exclusive permission resulted from an in-place Concurrent upgrade
             and other upgrade requests are still waiting, the downgrade cuts the current access context and reacquires Concurrent
             so that the remaining upgrade requests can continue acquiring Exclusive.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.ConcurrentToExclusive">
             <summary>
             Upgrades the currently held Concurrent permission to Exclusive.
             Upgrade requests take priority over ordinary Exclusive requests and execute serially until all have completed.
             </summary>
             <remarks>
             This method must be called from a context that currently holds Concurrent permission.
            
             After this call, the lock blocks new Concurrent entries into the current conversion window;
             callers that upgrade successfully execute in isolation under Exclusive semantics.
            
             After a successful upgrade, call <c>ReleaseExclusive()</c> to release it,
             or downgrade through <c>ExclusiveToConcurrent()</c>
             and then release according to the Concurrent protocol.
            
             Use this method to continue from a completed Concurrent phase into a continuous Exclusive access context,
             avoiding the access window caused by releasing Concurrent and then reacquiring Exclusive.
            
             When multiple Concurrent holders request an upgrade at the same time, their Exclusive regions execute serially.
             Too many upgrade requests may create substantial contention pressure;
             when the business logic can identify which callers actually need to upgrade by context or phase,
             prefer <see cref="M:IntomicLib.ConcurrentExclusiveLock.TryConcurrentToExclusiveWithSwitchContextID(System.Int32)"/>
             or <see cref="M:IntomicLib.ConcurrentExclusiveLock.TryConcurrentToExclusiveWithRaiseEpochID(System.Int32)"/>.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.TryConcurrentToExclusiveWithSwitchContextID(System.Int32)">
             <summary>
             While holding Concurrent permission, attempts to upgrade to Exclusive through SwitchContextID.
             Upgraded Exclusive requests block ordinary Exclusive acquisition until all upgrades have completed.
             When multiple Concurrent holders call this method with the same newContextID, only the holder that successfully switches ContextID can upgrade.
             When callers use different newContextID values, multiple callers may succeed, but their successful Exclusive regions are still isolated and ordered.
             On success, the caller holds Exclusive permission; on failure, the previously held Concurrent permission is released automatically.
             </summary>
             <remarks>
             This method must be called from a context that holds Concurrent permission.
            
             After this call, the lock blocks new Concurrent entries into the current conversion window,
             and callers that upgrade successfully execute in isolation under Exclusive semantics.
             ContextID Whether ContextID is switched is determined by <c>SwitchContextID(newContextID)</c>.
             
             After a successful upgrade, call <c>ReleaseExclusive()</c> to release it,
             or downgrade through <c>ExclusiveToConcurrent()</c>
             and then release according to the Concurrent protocol.
             
             After failure, the original Concurrent permission has already been released; do not call <c>ReleaseConcurrent()</c> again.
             Exclusive regions produced by multiple Concurrent upgrades remain serialized.
             </remarks>
             <param name="newContextID">
             The new business context ID to attempt to apply.
             </param>
             <returns>
             true True if ContextID was switched and Exclusive has been acquired;
             false false if ContextID did not change and the original Concurrent permission was released automatically.
             </returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock.TryConcurrentToExclusiveWithRaiseEpochID(System.Int32)">
             <summary>
             While holding Concurrent permission, attempts to upgrade to Exclusive through RaiseEpochID.
             Upgraded Exclusive requests block ordinary Exclusive acquisition until all upgrades have completed.
             The caller can advance the phase and upgrade successfully only when newEpochID is greater than the current EpochID.
             When newEpochID is less than or equal to the current EpochID, the upgrade fails and EpochID remains unchanged.
             When callers use different increasing newEpochID values, multiple callers may succeed, but their successful Exclusive regions are still isolated and ordered.
             On success, the caller holds Exclusive permission; on failure, the previously held Concurrent permission is released automatically.
             </summary>
             <remarks>
             This method must be called from a context that currently holds Concurrent permission.
            
             This method is suitable when EpochID represents a lifecycle, version number, or phase number.
             EpochID EpochID may advance only to a greater value; it cannot remain unchanged or move backward.
             After this call, the lock blocks new Concurrent entries into the current conversion window,
             and callers that upgrade successfully execute in isolation under Exclusive semantics.
             EpochID Whether EpochID advances is determined by <c>RaiseEpochID(newEpochID)</c>.
            
             After a successful upgrade, call <c>ReleaseExclusive()</c> to release it,
             or downgrade through <c>ExclusiveToConcurrent()</c>
             and then release according to the Concurrent protocol.
             
             After failure, the original Concurrent permission has already been released; do not call <c>ReleaseConcurrent()</c> again.
             Exclusive regions produced by multiple Concurrent upgrades remain serialized.
             </remarks>
             <param name="newEpochID">
             The new phase ID to attempt to advance to.
             </param>
             <returns>
             true True if EpochID was successfully advanced and Exclusive has been acquired;
             false false if EpochID was not advanced and the original Concurrent permission was released automatically.
             </returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLock._FreeRelease(System.Int64)">
            <summary>
            Helper method.
            </summary>
            <param name="counter">The delta to apply to the internal state counter.</param>
        </member>
        <member name="T:IntomicLib.ConcurrentExclusiveLockState">
            <summary>
            ConcurrentExclusiveLock lock state.
            </summary>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockState.Idle">
            <summary>
            Idle
            </summary>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockState.Concurrent">
            <summary>
            Concurrent
            </summary>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockState.Exclusive">
            <summary>
            Exclusive
            </summary>
        </member>
        <member name="T:IntomicLib.ConcurrentExclusiveLockCapacityExceededException">
            <summary>
            Indicates that the runtime number of Concurrent holders exceeded the internal 31-bit count capacity.
            This exception is not normally expected under real-world business conditions.
            </summary>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockCapacityExceededException.#ctor">
            <summary>
            Initializes a new
            <see cref="T:IntomicLib.ConcurrentExclusiveLockCapacityExceededException"/> <see cref="T:IntomicLib.ConcurrentExclusiveLockCapacityExceededException"/> instance.
            </summary>
        </member>
        <member name="T:IntomicLib.ConcurrentExclusiveLockPipeline">
             <summary>
             An access-permission pipeline for <see cref="T:IntomicLib.ConcurrentExclusiveLock"/>.
             </summary>
             <remarks>
             The Pipeline executes a sequence of synchronous business segments, each of which declares the access permission required for execution.
             During execution, it uses the permission successfully held by the preceding segment
             to automatically release, reacquire, continue, upgrade, or downgrade access.
            
             The specific semantics of each access mode are defined by <see cref="T:IntomicLib.ConcurrentExclusiveAccessMode"/>.
            
             When a try-type segment does not satisfy its execution condition, that segment is not executed;
             the Pipeline releases any access permission it still holds
             and continues processing subsequent segments from the None state.
            
             All Pipeline segments are synchronous, and a segment delegate must not cross an <c>await</c> boundary.
             Exceptions thrown by a segment delegate propagate to the caller, and subsequent segments are not executed;
             when the Pipeline exits, any access permission still held is released.
            
             This type is a value type, and a default-initialized instance is unusable.
             Create an instance by using the constructor that accepts a <see cref="T:IntomicLib.ConcurrentExclusiveLock"/>.
             </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockPipeline.Locker">
            <summary>
            The lock instance bound to the current pipeline.
            </summary>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockPipeline.#ctor(IntomicLib.ConcurrentExclusiveLock)">
            <summary>
            Creates an access-permission pipeline bound to the specified lock instance.
            </summary>
            <param name="locker">The lock instance to be used by the pipeline.</param>
            <remarks>
            A Pipeline continues to reuse this lock instance; multiple Pipeline instances may also be bound to the same lock instance.
            </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockPipeline.DoPipelineAsync(IntomicLib.ConcurrentExclusiveLockSegment[])">
            <summary>
            Executes a synchronous Pipeline operation on the thread pool.
            </summary>
            <remarks>
            This method does not make <see cref="T:IntomicLib.ConcurrentExclusiveLockSegment"/> asynchronous and does not perform asynchronous waits within a segment.
            It only schedules the synchronous <see cref="M:IntomicLib.ConcurrentExclusiveLockPipeline.DoPipeline(IntomicLib.ConcurrentExclusiveLockSegment[])"/> method to the thread pool through <see cref="M:System.Threading.Tasks.Task.Run(System.Action)"/>.
            If the caller is already running on a worker thread, thread-pool thread, or server request thread, <see cref="M:IntomicLib.ConcurrentExclusiveLockPipeline.DoPipeline(IntomicLib.ConcurrentExclusiveLockSegment[])"/> should normally be called directly.
            </remarks>
            <param name="segments">The Pipeline segments to execute in sequence.</param>
            <returns>A task representing this Pipeline execution.</returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockPipeline.DoPipeline(IntomicLib.ConcurrentExclusiveLockSegment[])">
             <summary>
             Executes a sequence of Pipeline segments in order.
             </summary>
             <remarks>
             Each segment declares the access permission required for its execution.
             Based on the permission successfully held by the preceding segment, the Pipeline
             automatically handles the permission state according to the definition of <see cref="T:IntomicLib.ConcurrentExclusiveAccessMode"/>.
            
             When a try-type segment does not satisfy its execution condition, that segment is not executed;
             the Pipeline releases any access permission it still holds
             and continues processing subsequent segments from the None state.
            
             Exceptions thrown by a segment delegate propagate to the caller, and subsequent segments are not executed;
             when this method exits, any access permission still held is released.
             </remarks>
             <param name="segments">The Pipeline segments to execute in sequence.</param>
        </member>
        <member name="T:IntomicLib.ConcurrentExclusiveAccessMode">
            <summary>
            The access-permission mode declared by a Pipeline segment.
            </summary>
            <remarks>
            Each enum value defines the access permission required by the current segment
            and how it is handled relative to the state successfully held by the preceding segment.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.None">
            <summary>
            No access permission.
            </summary>
            <remarks>
            Before the current segment runs, any permission still held by the preceding segment is released, and the current segment runs without access permission.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.Concurrent">
            <summary>
            Acquires an independent Concurrent permission segment.
            </summary>
            <remarks>
            If the preceding segment already holds Concurrent permission, it is still released and reacquired.
            To continue the current Concurrent context, use <see cref="F:IntomicLib.ConcurrentExclusiveAccessMode.ConvergeConcurrent"/>.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.TryConcurrent">
            <summary>
            Attempts to acquire an independent Concurrent permission segment.
            </summary>
            <remarks>
            If Concurrent permission is not acquired, the current segment is not executed and the remaining pipeline continues from the None state.
            If the preceding segment still holds permission, that permission is released before acquisition is attempted.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.Exclusive">
            <summary>
            Acquires an independent Exclusive permission segment.
            </summary>
            <remarks>
            Exclusive represents an independent exclusive-permission segment.
            If the preceding segment already holds Exclusive permission, it is still released and reacquired.
            To continue an existing Exclusive context, use <see cref="F:IntomicLib.ConcurrentExclusiveAccessMode.ConvergeExclusive"/>;
            to additionally condition execution of the current segment on a business ID, use <see cref="F:IntomicLib.ConcurrentExclusiveAccessMode.TryApplyIDConvergeExclusive"/>.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.TestExclusive">
            <summary>
            Attempts to acquire Exclusive permission only while the lock is Idle.
            </summary>
            <remarks>
            This mode does not preempt Concurrent access and does not wait for the lock state to change.
            If the preceding segment still holds permission, that permission is released before acquisition is attempted.
            The current segment is executed only when the lock is Idle and exclusive scheduling can be entered immediately;
            otherwise, the current segment is not executed and the remaining pipeline continues from the None state.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.TryExclusive">
            <summary>
            Attempts to acquire Exclusive permission preemptively.
            </summary>
            <remarks>
            This mode requests Exclusive permission preemptively and may wait.
            If the preceding segment still holds permission, that permission is released before acquisition is attempted.
            If a Concurrent-to-Exclusive upgrade request appears during contention, the current request may yield and fail.
            If Exclusive permission is not acquired, the current segment is not executed and the remaining pipeline continues from the None state.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.ConvergeConcurrent">
            <summary>
            Continues, downgrades to, or acquires Concurrent permission.
            </summary>
            <remarks>
            If the preceding segment already holds Concurrent permission, the current Concurrent context is continued and the current segment executes directly.
            If the preceding segment holds Exclusive permission, <c>ExclusiveToConcurrent()</c> is called to downgrade before executing the current segment.
            Under upgrade contention, the downgrade may cut the current access context and reacquire Concurrent permission.
            If no access permission is currently held, ordinary Concurrent permission is acquired.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.ConvergeExclusive">
             <summary>
             Continues, upgrades to, or acquires Exclusive permission.
             </summary>
             <remarks>
             If the preceding segment holds Concurrent permission, <c>ConcurrentToExclusive()</c> is called to upgrade in place before executing the current segment.
             If the preceding segment already holds Exclusive permission, the current Exclusive context is continued and the current segment executes directly.
             If no access permission is currently held, ordinary Exclusive permission is acquired.
            
             When multiple Concurrent holders request upgrades simultaneously, their upgraded Exclusive sections execute serially in sequence.
             Too many upgrade requests may create significant contention;
             when the business can use ContextID or EpochID to select which callers actually need to upgrade,
             consider using <see cref="F:IntomicLib.ConcurrentExclusiveAccessMode.TryApplyIDConvergeExclusive"/>.
             </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveAccessMode.TryApplyIDConvergeExclusive">
             <summary>
             Continues, upgrades to, or acquires Exclusive permission, conditioned on the result of applying a business ID.
             </summary>
             <remarks>
             The Try semantics of this mode apply to the result of applying the business ID; they do not imply that no waiting occurs.
            
             When ContextID is used, success means switching to a different business context;
             when EpochID is used, success means advancing to a greater business stage.
            
             If the business ID is applied successfully, the current segment executes with Exclusive permission.
             If the business ID is not applied, the current segment is not executed;
             the Pipeline releases any access permission it still holds
             and continues processing subsequent segments from the None state.
            
             When the preceding segment holds Concurrent permission, it attempts to upgrade to Exclusive subject to the business ID;
             when the preceding segment already holds Exclusive permission, the business ID is applied within the current Exclusive context;
             when the preceding segment holds no access permission, Exclusive permission is acquired after the business ID is applied successfully.
             </remarks>
        </member>
        <member name="T:IntomicLib.ConcurrentExclusiveLockSegment">
            <summary>
            A business segment in a Pipeline.
            </summary>
            <remarks>
            Each segment declares the access permission required to run it and stores the synchronous business code to execute.
            This type is a value type. A default-initialized instance cannot be executed; create business segments through the corresponding static factory methods.
            </remarks>
        </member>
        <member name="T:IntomicLib.ConcurrentExclusiveLockSegment.IDType">
            <summary>
            Specifies the business ID type represented by <see cref="F:IntomicLib.ConcurrentExclusiveLockSegment.ContextOrEpochID"/>.
            </summary>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockSegment.IDType.ContextID">
            <summary>
            Uses ContextID to switch the business context.
            </summary>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockSegment.IDType.EpochID">
            <summary>
            Uses EpochID to advance the business stage monotonically.
            </summary>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockSegment.Segment">
            <summary>
            The synchronous business code to be executed by the current segment.
            </summary>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockSegment.ContextOrEpochID">
            <summary>
            The business-context ID or business-stage ID to be applied by the current segment.
            </summary>
            <remarks>
            This value is meaningful only for <see cref="F:IntomicLib.ConcurrentExclusiveAccessMode.TryApplyIDConvergeExclusive"/>.
            When <see cref="F:IntomicLib.ConcurrentExclusiveLockSegment.IDKind"/> is <see cref="F:IntomicLib.ConcurrentExclusiveLockSegment.IDType.ContextID"/>, this value represents a ContextID;
            when <see cref="F:IntomicLib.ConcurrentExclusiveLockSegment.IDKind"/> is <see cref="F:IntomicLib.ConcurrentExclusiveLockSegment.IDType.EpochID"/>, it represents an EpochID.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockSegment.IDKind">
            <summary>
            Specifies whether <see cref="F:IntomicLib.ConcurrentExclusiveLockSegment.ContextOrEpochID"/> is a ContextID or an EpochID.
            </summary>
            <remarks>
            This value is meaningful only for <see cref="F:IntomicLib.ConcurrentExclusiveAccessMode.TryApplyIDConvergeExclusive"/>.
            </remarks>
        </member>
        <member name="F:IntomicLib.ConcurrentExclusiveLockSegment.Access">
            <summary>
            The access-permission mode declared by the current segment.
            </summary>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.None(System.Action)">
            <summary>
            Creates a business segment that requires no access permission.
            </summary>
            <param name="segment">The business code to execute without lock access.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.None(System.Func{System.Threading.Tasks.Task})">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.Concurrent(System.Action)">
            <summary>
            Creates a business segment that acquires independent Concurrent permission.
            </summary>
            <param name="segment">The business code to execute while Concurrent permission is held successfully.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.Concurrent(System.Func{System.Threading.Tasks.Task})">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.Exclusive(System.Action)">
            <summary>
            Creates a business segment that acquires independent Exclusive permission.
            </summary>
            <param name="segment">The business code to execute while Exclusive permission is held successfully.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.Exclusive(System.Func{System.Threading.Tasks.Task})">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.TryConcurrent(System.Action)">
            <summary>
            Creates a business segment that attempts to acquire independent Concurrent permission.
            </summary>
            <param name="segment">The business code to execute while Concurrent permission is held successfully.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.TryConcurrent(System.Func{System.Threading.Tasks.Task})">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.TestExclusive(System.Action)">
            <summary>
            Creates a business segment that attempts to acquire Exclusive permission only while the lock is Idle.
            </summary>
            <param name="segment">The business code to execute while Exclusive permission is held successfully.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.TestExclusive(System.Func{System.Threading.Tasks.Task})">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.TryExclusive(System.Action)">
            <summary>
            Creates a business segment that attempts to acquire Exclusive permission preemptively.
            </summary>
            <param name="segment">The business code to execute while Exclusive permission is held successfully.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.TryExclusive(System.Func{System.Threading.Tasks.Task})">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.ConvergeConcurrent(System.Action)">
            <summary>
            Creates a business segment that continues, downgrades to, or acquires Concurrent permission.
            </summary>
            <param name="segment">The business code to execute while Concurrent permission is held successfully.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.ConvergeConcurrent(System.Func{System.Threading.Tasks.Task})">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.ConvergeExclusive(System.Action)">
            <summary>
            Creates a business segment that continues, upgrades to, or acquires Exclusive permission.
            </summary>
            <param name="segment">The business code to execute while Exclusive permission is held successfully.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.ConvergeExclusive(System.Func{System.Threading.Tasks.Task})">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.TryApplyIDConvergeExclusive(System.Action,System.Int32,IntomicLib.ConcurrentExclusiveLockSegment.IDType)">
            <summary>
            Creates a business segment that converges to Exclusive permission, conditioned on the result of applying a business ID.
            </summary>
            <param name="segment">The synchronous business code to execute when the business ID is applied successfully and Exclusive permission is held.</param>
            <param name="contextOrEpochID">The business-context ID to switch to, or the business-stage ID to advance to.</param>
            <param name="idType">Specifies the business ID type represented by <paramref name="contextOrEpochID"/>.</param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockSegment.TryApplyIDConvergeExclusive(System.Func{System.Threading.Tasks.Task},System.Int32,IntomicLib.ConcurrentExclusiveLockSegment.IDType)">
            <summary>
            Disabled overload used to reject asynchronous pipeline segments at compile time.
            </summary>
            <param name="segment"></param>
            <param name="contextOrEpochID"></param>
            <param name="idType"></param>
            <returns></returns>
            <exception cref="T:System.NotSupportedException"></exception>
        </member>
        <member name="T:IntomicLib.ConcurrentExclusiveLockScope">
             <summary>
             A convenience wrapper for using <see cref="T:IntomicLib.ConcurrentExclusiveLock"/>.
             </summary>
             <remarks>
             During the lifetime of this scope, the caller may transition between Concurrent and Exclusive states according to the protocol,
             including Concurrent acquisition, Exclusive acquisition, in-place upgrade to Exclusive, and downgrade from Exclusive to Concurrent.
             The lock does not provide recursive nesting privileges.
            
             The caller may explicitly release the currently held access permission. If it is not released explicitly,
             <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> automatically performs the corresponding release based on the state currently held by the scope.
            
             Dispose releases only the access permission ultimately held by the scope; it does not restore ContextID or EpochID.
             ContextID and EpochID represent business state associated with the lock and must be set, switched, advanced, or restored by business code.
            
             This type simplifies lock-state management in a using scope and reduces release errors caused by exception paths, upgrade/downgrade paths, or early returns.
             A scope instance is intended for a single calling context, does not support concurrent use by multiple threads, and should not be copied.
             </remarks>
        </member>
        <member name="P:IntomicLib.ConcurrentExclusiveLockScope.ObservedState">
             <summary>
             An observational snapshot of the lock's current state.
             </summary>
             <remarks>
             This value reflects only the lock state observed at the instant of the read. It must not be treated as an exact state check and is intended only for diagnostics and monitoring.
            
             After a preemptive Exclusive request enters the contention window, this value may be observed as Exclusive
             even while the request is still waiting for current Concurrent holders to release.
             Therefore, ObservedState represents the lock's current access tendency or transition state;
             it does not mean that a thread is already executing Exclusive business code.
             </remarks>
        </member>
        <member name="P:IntomicLib.ConcurrentExclusiveLockScope.ObservedContention">
             <summary>
             Gets an observational indicator of the lock's current contention pressure.
             </summary>
             <remarks>
             This value is a snapshot of contention pressure observed at the instant of the read and is intended only for diagnostics, monitoring, or scheduling guidance.
            
             Therefore, the value is 0 in a purely Concurrent scenario;
             once Exclusive pressure exists, it reports the currently observed combined Concurrent and Exclusive pressure.
             </remarks>
        </member>
        <member name="P:IntomicLib.ConcurrentExclusiveLockScope.ContextID">
             <summary>
             Atomically gets or sets the business context ID associated with the current lock.
             </summary>
             <remarks>
             This property stores additional business state outside the lock protocol, such as an identifier for the current business context,
             allowing the same context to be recognized and redundant permission acquisition to be avoided.
            
             Setting this property directly unconditionally overwrites the current ContextID.
             To determine whether the context changed, use <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.SwitchContextID(System.Int32)"/>.
            
             A value of 0 indicates that no context ID is set.
             The meaning, allocation, validation, and cleanup of nonzero values are the caller's responsibility.
             </remarks>
        </member>
        <member name="P:IntomicLib.ConcurrentExclusiveLockScope.EpochID">
             <summary>
             Atomically gets or sets the business phase ID associated with the current lock.
             </summary>
             <remarks>
             This property stores additional business state outside the lock protocol, such as the lifecycle phase of an Exclusive region,
             a data version, a processing batch, or another business phase.
            
             Setting this property directly unconditionally overwrites the current EpochID.
             It does not check whether the phase advances and does not prevent rollback or reset.
             To require the phase ID to increase monotonically, use <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.RaiseEpochID(System.Int32)"/>.
            
             A value of 0 indicates that no phase ID is set.
             The meaning, allocation, validation, and cleanup of nonzero values are the caller's responsibility.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.SwitchContextID(System.Int32)">
            <summary>
            Sets a new ContextID and returns whether this operation changed the previous value.
            Returns false if the new value is equal to the previous value.
            </summary>
            <remarks>
            This method only switches the business context ID.
            </remarks>
            <param name="newContextID">The new context ID.</param>
            <returns>True if ContextID was set to a different value; false if the new value was equal to the previous value.</returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.RaiseEpochID(System.Int32)">
            <summary>
            Attempts to advance EpochID to a new phase and returns whether the advance succeeded.
            Returns false if the new value is less than or equal to the current value.
            </summary>
            <remarks>
            This method only advances the business phase ID.
            </remarks>
            <param name="newEpochID">The new phase ID. It must be greater than the current EpochID for the advance to succeed.</param>
            <returns>True if EpochID was successfully advanced to a greater value; otherwise, false.</returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.#ctor(IntomicLib.ConcurrentExclusiveLock)">
             <summary>
             Initializes a scope bound to the specified <see cref="T:IntomicLib.ConcurrentExclusiveLock"/>.
             </summary>
             <remarks>
             The constructor itself does not acquire any Concurrent or Exclusive access permission.
             The caller must explicitly acquire Concurrent or Exclusive permission, or perform an in-place upgrade or downgrade through this scope.
            
             <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> automatically performs the corresponding release based on the final state held by the scope,
             simplifying lock-state management within a using scope.
             </remarks>
             <param name="locker">
             The <see cref="T:IntomicLib.ConcurrentExclusiveLock"/> instance to be managed by this scope.
             </param>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.AcquireConcurrent(System.Int32)">
             <summary>
             Waits to acquire Concurrent access permission.
             </summary>
             <remarks>
             This method returns when entry into the Concurrent state is permitted.
            
             After acquiring Concurrent permission, the caller may explicitly release it later by calling <c>ReleaseConcurrent()</c>;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
             If the held state is later converted through another protocol, it must be released according to the converted state.
            
             <paramref name="maxConcurrent"/> limits the maximum Concurrent ID allowed for this acquisition.
             <paramref name="maxConcurrent"/> must not be less than 1.
             </remarks>
             <param name="maxConcurrent">
             The maximum allowed Concurrent ID.
             Valid return values are in the range [1, maxConcurrent].
             </param>
             <returns>
             Returns the Concurrent ID acquired by this operation, in the range [1, maxConcurrent].
             </returns>
             <exception cref="T:System.ArgumentException">Thrown when <paramref name="maxConcurrent"/> is less than 1.</exception>
             <exception cref="T:IntomicLib.ConcurrentExclusiveLockCapacityExceededException">
             Thrown when the number of simultaneously held Concurrent permissions exceeds the internal limit at runtime.
             The current implementation supports a 31-bit Concurrent count space; this limit is effectively unreachable in practical runtime environments.
             </exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.TryAcquireConcurrent(System.Int32)">
             <summary>
             Attempts to acquire Concurrent access permission.
             </summary>
             <remarks>
             This method makes a single attempt to enter the Concurrent state and does not wait for the lock state to change.
            
             A nonzero return value means Concurrent permission was acquired, and the caller may explicitly release it later by calling <c>ReleaseConcurrent()</c>;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
             If the held state is later converted through another protocol, it must be released according to the converted state.
            
             <paramref name="maxConcurrent"/> limits the maximum Concurrent ID allowed for this acquisition.
             <paramref name="maxConcurrent"/> must not be less than 1.
             </remarks>
             <param name="maxConcurrent">
             The maximum allowed Concurrent ID.
             Successful return values are in the range [1, maxConcurrent].
             </param>
             <returns>
             Returns the Concurrent ID acquired by this operation, in the range [1, maxConcurrent];
             returns 0 if Concurrent permission was not acquired.
             </returns>
             <exception cref="T:System.ArgumentException">Thrown when <paramref name="maxConcurrent"/> is less than 1.</exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.TryAcquireConcurrent(System.Int32,System.Int32)">
             <summary>
             Attempts to acquire Concurrent access permission within the specified time.
             </summary>
             <remarks>
             This method waits, within the specified timeout, for the lock to enter a state in which Concurrent permission can be acquired.
            
             A nonzero return value means Concurrent permission was acquired, and the caller may explicitly release it later by calling <c>ReleaseConcurrent()</c>;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
             If the held state is later converted through another protocol, it must be released according to the converted state.
            
             A <paramref name="millisecondsTimeout"/> value of 0 performs a single attempt without waiting; a negative value means an infinite wait.
            
             <paramref name="maxConcurrent"/> limits the maximum Concurrent ID allowed for this acquisition.
             <paramref name="maxConcurrent"/> must not be less than 1.
             If <paramref name="millisecondsTimeout"/> is greater than or equal to 0, the method waits at most until the timeout and then returns 0;
             if <paramref name="millisecondsTimeout"/> is negative, the method waits indefinitely.
             </remarks>
             <param name="millisecondsTimeout">
             The maximum number of milliseconds to wait for Concurrent permission.
             0 performs a single attempt without waiting; a negative value means an infinite wait.
             </param>
             <param name="maxConcurrent">
             The maximum allowed Concurrent ID.
             Successful return values are in the range [1, maxConcurrent].
             </param>
             <returns>
             Returns the Concurrent ID acquired by this operation, in the range [1, maxConcurrent];
             returns 0 if Concurrent permission was not acquired within the specified time.
             </returns>
             <exception cref="T:System.ArgumentException">Thrown when <paramref name="maxConcurrent"/> is less than 1.</exception>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.ReleaseConcurrent">
             <summary>
             Releases one currently held Concurrent access permission.
             </summary>
             <remarks>
             This method must be called while the current scope holds Concurrent permission.
            
             After this call, the current scope no longer holds that Concurrent access permission,
             and no business code that depends on that permission should continue to execute.
            
             A permission released explicitly will not be released again by <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/>.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.AcquireExclusive">
             <summary>
             Waits to acquire Exclusive access permission.
             </summary>
             <remarks>
             This method requests preemptive Exclusive permission.
            
             After the request is made, the lock blocks new Concurrent entrants
             and waits for current Concurrent holders to release.
             This method returns after the lock enters the Exclusive state.
            
             After acquiring Exclusive permission, the caller may explicitly release it later by calling <c>ReleaseExclusive()</c>,
             or downgrade it to Concurrent through <c>ExclusiveToConcurrent()</c> and then release it according to the Concurrent protocol;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.TryAcquireExclusive(System.Boolean)">
             <summary>
             Attempts to acquire Exclusive access permission.
             </summary>
             <remarks>
             When <paramref name="preemptConcurrent"/> is true,
             this method requests Exclusive permission preemptively.
             If Concurrent holders currently exist, the lock blocks new Concurrent entrants
             and participates in Exclusive contention;
             a successful Exclusive request waits for current Concurrent holders to release before acquiring Exclusive permission.
            
             When <paramref name="preemptConcurrent"/> is false,
             this method does not preempt Concurrent holders.
             It returns false immediately whenever Concurrent or Exclusive permission is currently held.
            
             A return value of true means Exclusive permission was acquired, and the caller may explicitly release it later by calling <c>ReleaseExclusive()</c>,
             or downgrade it to Concurrent through <c>ExclusiveToConcurrent()</c> and then release it according to the Concurrent protocol;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
            
             A return value of false means Exclusive permission was not acquired, and the caller must not execute business code that depends on Exclusive permission.
             </remarks>
             <param name="preemptConcurrent">
             true allows Concurrent holders to be preempted;
             false does not preempt Concurrent holders and can acquire Exclusive permission only when the lock is Idle.
             </param>
             <returns>
             true means Exclusive permission was acquired;
             false means Exclusive permission was not acquired.
             </returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.TryAcquireExclusive(System.Int32)">
             <summary>
             Attempts to acquire Exclusive access permission within the specified time.
             </summary>
             <remarks>
             This method attempts to acquire preemptive Exclusive permission within the specified timeout.
            
             A <paramref name="millisecondsTimeout"/> value of 0 performs a single attempt without waiting;
             a negative value means an infinite wait.
            
             A return value of true means Exclusive permission was acquired, and the caller may explicitly release it later by calling <c>ReleaseExclusive()</c>,
             or downgrade it to Concurrent through <c>ExclusiveToConcurrent()</c> and then release it according to the Concurrent protocol;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
            
             A return value of false means Exclusive permission was not acquired within the specified time,
             and the caller must not execute business code that depends on Exclusive permission.
             </remarks>
             <param name="millisecondsTimeout">
             The maximum number of milliseconds to wait for Exclusive permission.
             0 performs a single attempt without waiting; a negative value means an infinite wait.
             </param>
             <returns>
             true means Exclusive permission was acquired;
             false means Exclusive permission was not acquired within the specified time.
             </returns>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.ReleaseExclusive">
             <summary>
             Releases the currently held Exclusive access permission.
             </summary>
             <remarks>
             This method must be called while the current scope holds Exclusive permission.
            
             After this call, the current scope no longer holds Exclusive access permission,
             and no business code that depends on Exclusive permission should continue to execute.
            
             After the release completes, the lock returns to normal contention,
             and waiting Concurrent or Exclusive requests continue attempting to enter according to the lock's contention rules.
            
             To retain Concurrent access after ending Exclusive access,
             call <c>ExclusiveToConcurrent()</c>
             instead of this method.
            
             A permission released explicitly will not be released again by <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/>.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.ExclusiveToConcurrent">
             <summary>
             Downgrades the currently held Exclusive access permission to Concurrent access permission.
             </summary>
             <remarks>
             This method must be called while the current scope holds Exclusive permission.
            
             After this call, the current scope no longer holds Exclusive access permission,
             and no business code that depends on Exclusive permission should continue to execute.
            
             The current scope continues to hold Concurrent access permission,
             which may later be explicitly released by calling <c>ReleaseConcurrent()</c>;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
            
             This method preserves a continuous Concurrent access context after Exclusive modification is complete,
             avoiding the access window that would result from releasing Exclusive and then reacquiring Concurrent.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.ConcurrentToExclusive">
             <summary>
             Upgrades the current permission from Concurrent to Exclusive while Concurrent permission is held.
             Upgraded Exclusive requests prevent ordinary Exclusive requests from acquiring permission until all upgrades complete.
             </summary>
             <remarks>
             This method must be called while the current scope holds Concurrent permission.
            
             After this call, the lock blocks new Concurrent entrants from the current conversion window,
             and successfully upgraded callers execute in isolation under Exclusive semantics.
             The acquired permission may later be explicitly released by calling <c>ReleaseExclusive()</c>;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
            
             This method preserves a continuous Exclusive access context after the Concurrent phase is complete,
             avoiding the access window that would result from releasing Concurrent and then reacquiring Exclusive.
             Exclusive regions produced by multiple Concurrent upgrades remain serialized.
             A large number of upgrade requests may create significant contention pressure;
             when business logic can use a context or phase to select the callers that actually need to upgrade,
             prefer <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.TryConcurrentToExclusiveWithSwitchContextID(System.Int32)"/>
             or <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.TryConcurrentToExclusiveWithRaiseEpochID(System.Int32)"/>.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.TryConcurrentToExclusiveWithSwitchContextID(System.Int32)">
             <summary>
             While holding Concurrent permission, attempts to upgrade the current permission to Exclusive through SwitchContextID.
             Upgraded Exclusive requests prevent ordinary Exclusive requests from acquiring permission until all upgrades complete.
             When multiple Concurrent holders call this method with the same newContextID, only the holder that successfully switches ContextID can upgrade.
             When callers use different newContextID values, multiple callers may succeed separately, but their resulting Exclusive regions are still isolated and ordered.
             On success, the current scope holds Exclusive permission; on failure, the Concurrent permission held by the current scope is released automatically.
             </summary>
             <remarks>
             This method must be called while the current scope holds Concurrent permission.
            
             After this call, the lock blocks new Concurrent entrants from the current conversion window,
             and successfully upgraded callers execute in isolation under Exclusive semantics.
             Whether ContextID is switched successfully is determined by <c>SwitchContextID(newContextID)</c>.
            
             The acquired permission may later be explicitly released by calling <c>ReleaseExclusive()</c>;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
             After failure, the original Concurrent permission has already been released and <c>ReleaseConcurrent()</c> must not be called again.
             Exclusive regions produced by multiple Concurrent upgrades remain serialized.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.TryConcurrentToExclusiveWithRaiseEpochID(System.Int32)">
             <summary>
             While holding Concurrent permission, attempts to upgrade the current permission to Exclusive through RaiseEpochID.
             Upgraded Exclusive requests prevent ordinary Exclusive requests from acquiring permission until all upgrades complete.
             The caller can advance the phase and upgrade successfully only when newEpochID is greater than the current EpochID.
             When newEpochID is less than or equal to the current EpochID, the upgrade fails and EpochID remains unchanged.
             When multiple callers use distinct increasing newEpochID values, multiple callers may succeed separately, but their resulting Exclusive regions are still isolated and ordered.
             On success, the current scope holds Exclusive permission; on failure, the Concurrent permission held by the current scope is released automatically.
             </summary>
             <remarks>
             This method must be called while the current scope holds Concurrent permission.
            
             This method is suitable when EpochID represents a lifecycle, version number, or phase number.
             EpochID may advance only to a greater value; it cannot remain unchanged or move backward.
             After this call, the lock blocks new Concurrent entrants from the current conversion window,
             and successfully upgraded callers execute in isolation under Exclusive semantics.
             Whether EpochID is advanced successfully is determined by <c>RaiseEpochID(newEpochID)</c>.
            
             The acquired permission may later be explicitly released by calling <c>ReleaseExclusive()</c>;
             if it is not released explicitly, <see cref="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose"/> releases it according to the scope's final state.
             After failure, the original Concurrent permission has already been released and <c>ReleaseConcurrent()</c> must not be called again.
             Exclusive regions produced by multiple Concurrent upgrades remain serialized.
             </remarks>
        </member>
        <member name="M:IntomicLib.ConcurrentExclusiveLockScope.Dispose">
            <summary>
            Releases any access permission still held by this scope.
            </summary>
            <remarks>
            Dispose releases Concurrent or Exclusive permission solely according to the final held state recorded by the scope.
            Permissions already released explicitly are not released again.
            Dispose does not restore or clear ContextID or EpochID.
            </remarks>
        </member>
    </members>
</doc>
